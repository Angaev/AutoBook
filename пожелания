1. Показать Алексею Коптелину. Причесать внешний вид. [V]
2. На всех формах должен быть фидбек. [ ]
3. Добавить валидацию форм на стороне JS. [V]
4. Удаление издательства. Показывать список, в нем выбираем элемент для удаления. [V]
   То же касается переименования. [V]
5. Like. Сделать через Ajax. [V]
6. Перегруппировать страницу редактирования профиля. [V]
   Сделать загрузку автара асинхронной. [ ]
7. Реализовать полнотекстовый поиск по книгам. (Sphinx). [ ]



CREATE OR REPLACE
VIEW `book_info` AS 
SELECT 
  books.id, books.name, books.year,
  IFNULL(books.image, "img/book_cover/no_cover.png") as image,
    (SELECT COUNT(DISTINCT id) FROM likes WHERE likes.book_id = books.id AND likes.actual = 1) AS likeCount, 
    (SELECT COUNT(DISTINCT id) FROM comments WHERE comments.book_id = books.id AND comments.actual = 1) AS commentCount,
  book_description.description
FROM
  books
LEFT JOIN publishing_houses ON books.publishing_house_id = publishing_houses.id 
LEFT JOIN book_description ON book_description.book_id = books.id